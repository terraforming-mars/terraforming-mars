{
    "compilerOptions": {
        "outDir": "../build/src",
    },
    "extends": "../tsconfig.json",
    "include": [
        "**/*.ts",
        "genfiles/*.json",
        "server/ma/*.json",
    ],
    "exclude": [
      "client/**/*.ts"
    ]
}
